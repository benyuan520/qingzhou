<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="icu.navergo.demo.Uml.mapper.UserMapper">
    <select id="all" resultType="icu.navergo.demo.Uml.entity.User">
        select * from user;
    </select>
    <select id="selectname" resultType="icu.navergo.demo.Uml.entity.User">
        select jb_name from jb;
    </select>
</mapper>
